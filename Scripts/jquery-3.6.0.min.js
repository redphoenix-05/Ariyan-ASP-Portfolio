/*
 * jQuery 3.6.0 Placeholder
 * This file serves as a placeholder for jQuery.
 * The actual jQuery library will be loaded from CDN via ScriptResourceMapping.
 * If CDN fails, this placeholder ensures the application doesn't break.
 */

// Basic jQuery placeholder to prevent undefined errors
if (typeof jQuery === 'undefined') {
    window.jQuery = window.$ = function(selector) {
        console.warn('jQuery is loading from CDN. Please ensure internet connectivity.');
        return {
            ready: function(fn) { 
                if (document.readyState === 'loading') {
                    document.addEventListener('DOMContentLoaded', fn);
                } else {
                    fn();
                }
            },
            val: function() { return ''; },
            hide: function() { return this; },
            show: function() { return this; },
            addClass: function() { return this; },
            removeClass: function() { return this; },
            attr: function() { return this; },
            prop: function() { return this; },
            on: function() { return this; },
            off: function() { return this; },
            click: function() { return this; }
        };
    };
    
    // Mark as placeholder
    jQuery.isPlaceholder = true;
}